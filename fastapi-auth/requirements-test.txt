# Testing Dependencies for VoicePartnerAI
# Install with: pip install -r requirements-test.txt

# Core testing framework
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0
pytest-mock>=3.12.0
pytest-xdist>=3.5.0              # Parallel test execution
pytest-html>=4.1.1               # HTML test reports
pytest-json-report>=1.5.0        # JSON test reports for CI
pytest-benchmark>=4.0.0          # Performance benchmarking

# FastAPI testing
httpx>=0.25.2                    # Async HTTP client for testing
fastapi[all]>=0.104.1

# Database testing
pytest-postgresql>=5.0.0         # PostgreSQL test fixtures (if using PostgreSQL)
alembic>=1.12.1                  # Database migrations

# Mocking and fixtures
factory-boy>=3.3.0               # Test data factories
faker>=20.1.0                    # Fake data generation
responses>=0.24.1                # HTTP request mocking
pytest-freezegun>=0.4.2          # Time mocking
unittest-mock>=1.0.1             # Mock objects

# Code quality and coverage
coverage[toml]>=7.3.2            # Coverage measurement
pytest-clarity>=1.0.1            # Better assertion output
pytest-sugar>=0.9.7              # Better test output formatting

# Load testing (optional)
locust>=2.17.0                   # Load testing framework

# Security testing
bandit>=1.7.5                    # Security linting
safety>=2.3.5                    # Dependency vulnerability scanning

# Performance testing
memory-profiler>=0.61.0          # Memory usage profiling
py-spy>=0.3.14                   # Python profiler

# Utilities
freezegun>=1.2.2                 # Time mocking
pydantic-factories>=1.17.3       # Pydantic model factories

# Development utilities (useful for debugging tests)
ipdb>=0.13.13                    # Debugger
rich>=13.7.0                     # Beautiful terminal output

# Type checking for tests
mypy>=1.7.1
types-requests>=2.31.0.10