# Enhanced Requirements for Resilient VoicePartnerAI Application
# Add these to your existing requirements.txt

# Core FastAPI and web framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
httpx>=0.25.0

# Error handling and resilience
tenacity>=8.2.3          # Retry logic with exponential backoff
backoff>=2.2.1           # Alternative retry library
circuitbreaker>=1.4.0    # Circuit breaker pattern

# External service integrations
openai>=1.3.0            # OpenAI API client
twilio>=8.10.0           # Twilio API client
stripe>=7.8.0            # Stripe payment processing
elevenlabs>=0.2.0        # ElevenLabs TTS (if available)

# HTTP client improvements
aiohttp>=3.9.0           # Alternative async HTTP client
requests>=2.31.0         # Synchronous HTTP client (for some services)

# Monitoring and logging
structlog>=23.2.0        # Structured logging
prometheus-client>=0.19.0 # Metrics for Prometheus
sentry-sdk[fastapi]>=1.38.0 # Error tracking and monitoring

# Database resilience
sqlalchemy[asyncio]>=2.0.23
alembic>=1.12.1
psycopg2-binary>=2.9.9   # PostgreSQL adapter with connection pooling
redis>=5.0.1             # Caching and session storage

# Health checks and monitoring
psutil>=5.9.6            # System metrics
python-multipart>=0.0.6 # File upload support

# Configuration and environment
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Security enhancements
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6

# Development and testing
pytest>=7.4.3
pytest-asyncio>=0.21.1
httpx>=0.25.0            # For testing async endpoints
faker>=20.1.0            # Test data generation

# Optional: APM and monitoring tools
# elastic-apm>=6.20.0    # Elastic APM
# datadog>=0.49.1        # Datadog monitoring
# newrelic>=9.4.0        # New Relic monitoring